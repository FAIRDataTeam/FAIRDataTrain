# Normative architecture documents (copied 12 September 2026)

These are copies of the documents in the project folder `FAIR Data Train/fair-data-station-architecture/`, placed here so the repository is self-contained. **The project folder is the editing location**; when a document changes there, copy it here again (and re-extract `../adr/ADR-001…027` if an ADR changed).

| File | What it is |
|---|---|
| `fdt-ecosystem-architecture.md` | Ecosystem architecture v0.1.4 — components, roles, networks, run model, shared contracts, **ADR-014–027** |
| `fair-data-station-architecture.md` | Station component architecture v0.3 — SPI, checkpoints, agreement lifecycle, profiles, **ADR-001–013** |
| `fdt-itinerary-patterns.md` | Itinerary patterns P1–P10; §9 failure and partial-result semantics per pattern (ADR-026) |
| `fdt-use-case-time-to-groin.md` | The reference case (fictional stations; sourced Dutch stroke-care norms — never a "< 2 h" target) |
| `fdt-data-space-alignment.md` | Requirements register A–H against IDSA Rulebook / DSP / DCP / Gaia-X / DSSC / Data Act / DGA / EHDS |
| `fdt-implementation-plan.md`, `AGENT-BRIEF.md` | The implementation plan (M0–M4, WP-0.1–4.4, decisions D1–D5) and the agent's entry brief |
| `fair-data-station-ui-design-brief.md`, `mockups/` | The July UI brief and the 22 console mock-ups (`.dc.html` screens, generator, PNG renders, README) — the UI specification |
| `diagrams/` | ArchiMate/UML/component diagrams (SVG/PNG) with PlantUML sources |

The individual decision records are in [`../adr/`](../adr/).
